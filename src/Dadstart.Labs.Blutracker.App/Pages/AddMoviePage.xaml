<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Dadstart.Labs.Blutracker.Pages.AddMoviePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="{Binding PageTitle}">

    <VerticalStackLayout Padding="16" Spacing="12">
        <Label FontSize="18" Text="Movie details" />

        <Entry Placeholder="Title" Text="{Binding Title}" />
        <Entry Keyboard="Numeric" Placeholder="Release year (optional)" Text="{Binding ReleaseYear}" />

        <HorizontalStackLayout Spacing="12">
            <Button Text="Cancel" Command="{Binding CancelCommand}" />
            <Button Text="Save" Command="{Binding SaveCommand}" />
        </HorizontalStackLayout>
    </VerticalStackLayout>
</ContentPage>

